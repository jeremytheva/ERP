export default function RolePage() {
  return (
    <div className="space-y-4 rounded-lg border border-dashed border-border/60 bg-muted/10 p-6 text-sm text-muted-foreground">
      <p>Select a role-specific module from the sidebar to get started.</p>
      <p className="text-xs">This page intentionally left minimal pending role dashboards.</p>
    </div>
  );
}
